<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Text styles -->
    <style name="TextBase">
        <item name="android:layout_width">wrap_content</item>

        <item name="android:gravity">center</item>
    </style>

    <style name="Header" parent="TextBase">
        <item name="android:textSize">@dimen/header_textSize</item>
    </style>

    <style name="TodayTemp" parent="TextBase">
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/today_temp_textSize</item>
        <item name="android:layout_gravity">center</item>
    </style>

    <style name="TodayKind" parent="TodayTemp">
        <item name="android:textSize">@dimen/today_kind_textSize</item>
    </style>

    <style name="ExtraBlockValue" parent="TextBase">
        <item name="android:textSize">@dimen/extra_block_value_textSize</item>
    </style>

    <style name="ExtraBlockDescription" parent="TextBase">
        <item name="android:textSize">@dimen/extra_block_description_textSize</item>
    </style>



    <!--  -->

    <style name="ExtraBlock">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
    </style>
</resources>
